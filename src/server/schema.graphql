type Query {
	job(id: ID!): Job
	jobs: [Job!]
	company(id: ID!): Company
}

type Company {
	id: ID!
	name: String
	description: String
	jobs: [Job!]
}

type Job {
	id: ID!
	company: Company!
	title: String!
	description: String
}
